% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{available_daily_flows}
\alias{available_daily_flows}
\title{Retrieve daily aggregated flow and level data}
\usage{
available_daily_flows(
  station_number,
  start_date,
  end_date,
  source = NULL,
  fun = mean
)
}
\arguments{
\item{station_number}{Character vector of station identifiers.}

\item{start_date}{Start of the requested period (Date, or character
coercible to Date).}

\item{end_date}{End of the requested period (Date, or character coercible
to Date).}

\item{source}{Optional single character string naming the adapter to use
directly. When \code{NULL} (default) the router scans all registered adapters.}

\item{fun}{Aggregation function applied as \code{fun(value, na.rm = TRUE)}.
Defaults to \code{\link[=mean]{mean()}}.}
}
\value{
A tibble with columns \code{station_number} (chr), \code{date} (Date),
\code{value} (dbl), \code{parameter} (chr), \code{units} (chr), \code{source} (chr),
\code{approval} (chr), and \code{quality_flag} (chr).
}
\description{
Calls \code{\link[=available_flows]{available_flows()}} then aggregates sub-daily observations into one
value per station per calendar day using \code{fun}.
}
