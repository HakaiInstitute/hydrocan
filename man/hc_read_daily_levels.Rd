% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{hc_read_daily_levels}
\alias{hc_read_daily_levels}
\title{Retrieve daily water level summaries}
\usage{
hc_read_daily_levels(
  station_id,
  start_date,
  end_date = Sys.Date(),
  source = NULL
)
}
\arguments{
\item{station_id}{Character vector of station identifiers.}

\item{start_date}{Start of the requested period (Date, or character
coercible to Date).}

\item{end_date}{End of the requested period (Date, or character coercible
to Date). Defaults to today.}

\item{source}{Optional single character string naming the data source to use
directly. When \code{NULL} (default) the source is detected automatically from
the station ID. See \code{\link[=hc_list_sources]{hc_list_sources()}} for available names.}
}
\value{
A tibble with columns \code{station_id} (chr), \code{date} (Date),
\code{value} (dbl), \code{parameter} (chr: \code{"water_level"}), \code{unit} (chr),
\code{provider_name} (chr), \code{approval} (chr), and \code{quality_flag} (chr).
}
\description{
Returns one water level value per station per calendar day as published by
the data source. Not all data sources publish daily level data; those that
do not will produce a warning and return no rows for the affected stations.
}
